import AboutSection from '@/components/AboutSection';

export default function AboutPage() {
    return (
        <div className="min-h-[calc(100vh-theme(spacing.20)-theme(spacing.24))] flex flex-col">
            <AboutSection />
        </div>
    );
}
